<%- if controller_name != 'sessions' %>
  <p><%= link_to "Entrar", new_session_path(resource_name), class: "link link-primary" %></p>
<% end %>

<%- if devise_mapping.registerable? && controller_name != 'registrations' %>
  <p><%= link_to "Criar conta", new_registration_path(resource_name), class: "link link-primary" %></p>
<% end %>

<%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
  <p><%= link_to "Esqueci a senha", new_password_path(resource_name), class: "link link-primary" %></p>
<% end %>

<%- if devise_mapping.confirmable? && controller_name != 'confirmations' %>
  <p><%= link_to "Não recebi a confirmação", new_confirmation_path(resource_name), class: "link link-primary" %></p>
<% end %>

<%- if devise_mapping.lockable? && resource_class.unlock_strategy_enabled?(:email) && controller_name != 'unlocks' %>
  <p><%= link_to "Não recebi o desbloqueio", new_unlock_path(resource_name), class: "link link-primary" %></p>
<% end %>

<%- if devise_mapping.omniauthable? %>
  <%- resource_class.omniauth_providers.each do |provider| %>
    <p><%= button_to "Entrar com #{OmniAuth::Utils.camelize(provider)}", omniauth_authorize_path(resource_name, provider), class: "btn btn-outline btn-sm", data: { turbo: false } %></p>
  <% end %>
<% end %>
